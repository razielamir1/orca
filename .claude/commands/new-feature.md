Plan and implement a new feature using the full agent pipeline.

Follow this sequence:
1. `@architect` — Design the feature architecture. Present the plan and wait for user to type PROCEED before continuing.
2. `@database-expert` — Create any necessary database schemas and migrations.
3. `@backend-developer` — Build the API endpoints and server-side logic.
4. `@frontend-developer` — Implement the frontend logic, state management, and API integration.
5. `@ui-designer` — Design and style the UI components.
6. `@qa-expert` — Run tests and scan for bugs. Write report to `.claude/audits/AUDIT_QA.md`.

Feature to build: $ARGUMENTS
